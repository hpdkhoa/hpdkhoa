CREATE OR REPLACE PROCEDURE INS_LOGIN_USER_DUMP(AMOUNT INT)
AS
$$
BEGIN
	FOR i IN 1..AMOUNT LOOP
		INSERT INTO USERLOGIN
			(USERNAME, PASSWORD)
		( SELECT 'Test' || (SELECT last_value FROM userlogin_id_seq),
			'202cb962ac59075b964b07152d234b70');
	END LOOP;
END;
$$ LANGUAGE PLPGSQL;

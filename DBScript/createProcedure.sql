CREATE OR REPLACE PROCEDURE INS_LOGIN_USER_DUMP(AMOUNT INT)
AS
$$
BEGIN
	FOR i IN 1..AMOUNT LOOP
		INSERT INTO USERLOGIN
			(USERNAME, PASSWORD)
		( SELECT 'Test' || COALESCE(MAX(ID),1), '202cb962ac59075b964b07152d234b70'
		 FROM USERLOGIN);
	END LOOP;
END;
$$ LANGUAGE PLPGSQL;
